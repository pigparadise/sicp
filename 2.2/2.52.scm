;; i don't know how to draw on my computer, canvas??? 
;; i can't test my thought, give up T_T

;; c)
;; (define (square-limit painter n)
;;   (let ((top (beside (tl painter)
;;                      (tr (filp-horiz painter))
;;              )
;;         )
;;         (bottom (beside (bl (filp-vert painter))
;;                         (br (filp-vert (filp-horiz painter)))
;;                 )
;;         )
;;        )
;;  )
;; )